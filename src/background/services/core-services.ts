export enum BGCoreServices {
    CONFIG = "configService",
    HTTP = "httpService",
    MESSAGING = "messagingService",
    PLUGIN_MESSAGING = "pluginMessagingService",
    NOTIFICATION = "notificationService",
    WINDOW = "windowService",
    STORAGE = "storageService",
}
